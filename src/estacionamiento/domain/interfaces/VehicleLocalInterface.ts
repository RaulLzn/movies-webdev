/** 
 * @interface VehicleLocalInterface
 * @description Interfaz que representa la estructura local de un vehículo.
 * @property {string} placa - La placa del vehículo.
 * @property {string} tipo - El tipo de vehículo (Carro, Moto, Camioneta).
 */
export interface VehicleLocalInterface {
  placa: string;
  tipo: string;
}